{
  "name": "erp",
  "version": "0.1.0",
  "private": true,
  "description": "ERP multiplataforma con React Native Web y API REST.",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "build": "npm run build:backend",
    "build:backend": "npm --prefix backend run build",
    "test": "npm --prefix backend test",
    "audit:closure": "node scripts/closure-audit.mjs",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "audit:dependencies": "npm audit --audit-level=high && npm --prefix backend audit --audit-level=high && npm --prefix frontend audit --audit-level=high"
  },
  "dependencies": {
    "mongodb": "^7.6.0",
    "mongoose": "^9.10.2"
  }
}
